spring:
  profiles:
    active: ${PROFILE:local}  # ????? ?? ???? ???
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

server:
  port: ${SERVER_PORT:8090}
